#include<stdio.h>
#include<stdlib.h>
#define max 100
int n;
int queue[max];
struct node{
	int data;
	struct node*next;
};
struct node*front=NULL;
struct node*rear=NULL;
void enqueue()
{
	struct node*ptr,*next;
	
}
